output: "build/dist"  # 必须添加的字段
releases:
  - name: linux
    jobs:
      - name: appimage
        package:
          app_id: "top.wherewego.vnt_app"  # 移动到package下
          name: "vnt_app"                 # 移动到package下
          version: "1.2.16"               # 移动到package下
          platform: "linux"
          target: "appimage"               # 必须添加的字段
          icon: "assets/app_icon.png"      # 保持在这里
